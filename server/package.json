{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "start:dev": "nodemon --watch .env --ext js,mjs,json,env app.js",
    "client": "cd ../client && npm start",
    "dev": "concurrently \"npm run start:dev\" \"npm run client\"",
    "build": "cd ../client && npm install && npm run build"
  },
  "author": "kobi",
  "license": "ISC",
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "axios": "^1.16.0",
    "bcrypt": "^5.0.0",
    "concurrently": "^5.3.0",
    "cross-env": "^10.1.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "joi": "^17.4.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.11.10",
    "nodemon": "^2.0.7"
  }
}
